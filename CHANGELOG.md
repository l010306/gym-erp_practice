# Project Change Log

All notable changes to the Gym ERP project for production readiness will be documented in this file.

## [Unreleased] - 2026-03-26

### Added
- [x] Multi-stage `Dockerfile` for Java 21 Spring Boot.
- [x] `docker-compose.yml` for unified deployment.

### Changed
- [x] Spring Boot version downgraded from `4.0.1` to `3.4.1` (stable LTS).
- [x] Corrected Maven starter names in `pom.xml` (`spring-boot-starter-web`, `spring-boot-starter-test`).
- [x] Database configuration updated to use environment variables (`SPRING_DATASOURCE_*`).
- [x] `spring.jpa.hibernate.ddl-auto` set to `validate` (default).
- [x] `SequenceFixer` restricted to `local` profile to avoid accidental use in production.

### Removed
- [x] `PasswordMigration.java`: Removed security risk of auto-setting default passwords.
- [x] Hardcoded database credentials from `application.properties`.

---
*Generated by Antigravity*
